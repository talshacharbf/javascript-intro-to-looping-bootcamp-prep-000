function forloop (array) {
  var array = {};
    let i = 0; i < 25; i++) {
      if (i = 1) {
        array.push( "I am 1 strange loop.")}
          else {
            array.push( `I am `${i}` stange Loops.`)
              }
                }
